SYNOPSIS

A simple script to add ca-certificates to the Android certificate store.

HOWTO
1) install adb
2) connect your phone with USB debugging enabled
3) execute add-certs-to-store.sh
4) reboot the phone
